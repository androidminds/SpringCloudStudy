server:
  port: ${port:8200}

eureka:
  client:
    serviceUrl:
      defaultZone: ${eureka.serviceUrl:http://127.0.0.1:8761/eureka/}

spring:
  application:
    name: ConsumerFeign

feign:
  hystrix:
    enabled: true

